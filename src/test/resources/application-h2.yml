spring:
  datasource:
    url: jdbc:h2:mem:campus_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration

pagehelper:
  helper-dialect: h2

app:
  media-root: ./target/test-media
